import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mohan.attendance',
  appName: 'AttendanceApp',
  webDir: 'dist/mobile-app/browser'
};

export default config;
